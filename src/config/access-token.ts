export const cookieName = "access-token";

/**
 * Expiration time in milliseconds for the access token.
 */
export const expiresTime = 1000 * 60 * 60 * 24 * 30; // <- Represents 30 days (1000 milliseconds * 60 seconds * 60 minutes * 24 hours * 30 days).
